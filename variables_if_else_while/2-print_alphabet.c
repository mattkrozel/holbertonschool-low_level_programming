#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	int l;

	for (int l = 'a'; l <= 'z'; l++)
	{
		putchar(tolower(l'\n'));
	}

	return (0);
}
