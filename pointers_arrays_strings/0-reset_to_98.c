#include <unistd.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * reset_to_98 - check the code
 * @n : input
 * Description: takes pointer to an int as parameter and updates
 * value it points to to 98
 * Return: 98
 */
void reset_to_98(int *n)
{
	int reset = 98;
	int *n = &reset;
}
