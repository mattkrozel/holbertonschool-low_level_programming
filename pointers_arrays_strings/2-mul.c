#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * mul - Check main
 * @a: An input number
 * @b: an inout number
 * Description: function that multiplies two integers
 * Return: result
 */
int mul(int a, int b)
{

	return (a * b);
}
