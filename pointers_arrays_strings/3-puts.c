#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"

/**
 * _puts - check the code
 * @str: input
 * Description: prints string followed by new line to stdout
 * Return: return 0
 */
void _puts(char *str)
{
	puts(str);
}
