#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - Check main
 * @s: An input number
 * Description: function that multiplies two integers
 * Return: result
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
