#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - Check main
 * @r: An input number
 * Description: function that computes the absolute value of an integer
 * Return: the absolute value
 */
int _abs(int r)
{
	int val;

	val = abs(r);
	return (val);
}
